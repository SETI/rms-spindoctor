schema_version: 1
# COISS | VGISS | GOSSI | NHLORRI
mission: COISS
# NAC | WAC | SSI | NA | WA | LORRI
camera: WAC
image_id: W1580760393_1_CALIB
image_datetime_utc: '2008-02-03T19:29:51.952'
exposure_time_sec: 26.0000
# canonicalized: filters sorted, '+'-joined
filter_combo: 'CL1+CL2'
image_url: 'pds3://calibrated/COISS_2xxx/COISS_2041/data/1580756433_1580830157/W1580760393_1_CALIB.IMG'

# First tag is the primary class; must match the directory the
# sidecar lives in.
scene_tags:
  - star_dominated

ground_truth:
  offset_dv_px: -2.4821
  offset_du_px: -3.1711
  # 1sigma marginal; tighten for bright stars / sharp limbs.
  offset_uncertainty_px: 1.0
  source: operator_verified
  operator: rfrench
  verified_date: 2026-07-13
  ui_version: 'spindoctor 0.1.dev63'
  notes: |
    Cassini WAC clear-filter star field, 26 s exposure. Navigable: the
    catalog stars align with detections at this offset, operator-verified
    in the manual-nav dialog (2026-07-13). An earlier version of this
    entry expected status failed (from a period when a broken star gate
    could not close the frame); ground truth and expected were re-saved
    to success/high.

    Re-ratchet 2026-07-20 (sidecar reconciled to the seed-20260718 recalibration; attribution basis util/calibration/CAMPAIGN_20260718.md). Expected success/high/StarFieldFromCatalogNav -> measured success/high/StarRefineNav. Primary re-ratcheted StarFieldFromCatalogNav->StarRefineNav: the refit re-balanced the star-field terms so the 95-star refine (0.9513) edges the field matcher 0.95 hard cap. A multi-star refine legitimately carries the pick; status/tier/offset unchanged.

    Ground truth corrected by +0.500 / +0.500 px in (dv, du) for issue #635:
    the star model predicted positions in oops uv while the techniques
    measured them in array indices, so every star-derived offset was short
    by half a pixel on each axis. This ground truth is star-derived -- the
    sidecar above records it as a Star technique's own offset, or records
    that only Star techniques ran -- so it carries that bias in full, and
    the correction is the datum itself rather than the amount this frame's
    current answer happened to move. The operator verification stands: the
    star boxes the overlay drew came from the same biased conversion, and
    half a pixel is below what an overlay resolves, so verification never
    constrained this value either way.

expected:
  # success | failed | conflicted
  status: success
  # high | medium | low | failed
  confidence_tier: high
  # e.g. BodyLimbNav
  primary_technique: StarRefineNav
  techniques_must_run: [StarFieldFromCatalogNav]
  techniques_must_skip: []
